# FindBack AI Service Environment Variables
# Copy this file to .env and customize if needed

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the AI service (default: 3300)
# Must be in the assigned range 3300-3399
PORT=3300

# Host binding (use 0.0.0.0 for Docker)
HOST=0.0.0.0

# Number of workers (keep at 1 for CPU inference)
WORKERS=1

# =============================================================================
# Model Configuration (Advanced - defaults are recommended)
# =============================================================================

# CLIP Model name (options: ViT-B/32, RN50, RN101, ViT-B/16, ViT-L/14)
# ViT-B/32 is recommended for balance of speed and accuracy
# CLIP_MODEL_NAME=ViT-B/32

# Device for inference (cpu or cuda)
# Use cpu for server deployment without GPU
# DEVICE=cpu

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO
